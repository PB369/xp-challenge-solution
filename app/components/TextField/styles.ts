import { mainGray } from "@/assets/colors/colors";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    fieldHolder: {
        width: "100%",
        height: 32,
        backgroundColor: mainGray,
        borderRadius: 32
    }
})